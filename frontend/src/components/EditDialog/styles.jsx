import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  form: {
    margin: "0 2rem",
    textAlign: "center",
    padding: "2rem 0",
  },
  inline: {
    padding: "2rem 2rem",
  },
}));
